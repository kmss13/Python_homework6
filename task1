
#Заполните массив элементами арифметической прогрессии. Её первый элемент,
#разность и количество элементов нужно ввести с клавиатуры. Формула для
#получения n-го члена прогрессии: an = a1 + (n-1) * d.
#Каждое число вводится с новой строки.

list=[]
n=int(input("Введите кол-во элементов прогрессии"))
a=int(input("Введите первое число прогрессии"))
d=int(input("Введите разность чисел прогресии"))
for i in range(1, n+1):
    an = a + (i-1) * d
    list.append(an)
print(list)  
